- type: entity
  abstract: true
  parent: [BaseItem, PowerCellSlotSmallItem, RecyclableItemDeviceSmall] # Frontier: added RecyclableItemDeviceSmall
  id: BaseHandheldComputer
  components:
  - type: ActivatableUIRequiresPowerCell
  - type: ItemToggle
    onUse: false # above component does the toggling
    onActivate: false # VRS: prevent ItemToggle from consuming activation intended for handheld UI
  - type: PowerCellDraw
    drawRate: 0
    useRate: 20
  - type: ToggleCellDraw
